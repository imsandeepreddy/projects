global:
  scrape_interval: 5s # Fast scrape interval for testing and debugging

scrape_configs:
  - job_name: 'flask-app'
    static_configs:
      - targets: ['app:5000'] # Points directly to the app container

  - job_name: "node"
    static_configs:
      - targets: ["node-exporter:9100"]
